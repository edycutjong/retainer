## Summary

<!-- What does this PR change, and why? The "why" is the part that matters. -->

## Changes

-

## Checklist

- [ ] `yarn ci` passes (compile, contract tests, unit tests, lint ×2, typecheck ×2)
- [ ] `yarn e2e` passes (Playwright, no keys required)
- [ ] Tests added or updated for the change — and any regression test is **named after the
      defect it pins**
- [ ] No secret, key, account id, or `.env` value added to the tree
- [ ] Docs updated if behavior changed (`README.md`, `docs/`, `JUDGE.md`)

## On-chain impact

<!-- Does this change RetainerAccess.sol? If so: was it redeployed, and is
     packages/nextjs/contracts/deployedContracts.ts pointing at the new address? -->

- [ ] No contract change
- [ ] Contract changed — redeployed and `deployedContracts.ts` updated

## Related issues

Closes #
